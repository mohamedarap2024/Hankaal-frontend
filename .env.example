# Leave empty in production — Vercel proxies /api to Render (see vercel.json)
VITE_API_URL=

# Must match API_SECRET on backend
VITE_API_KEY=change-this-to-a-long-random-api-secret

# Optional — Google Sign-In
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
